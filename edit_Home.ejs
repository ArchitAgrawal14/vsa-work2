<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXhW+ALEwIH"
      crossorigin="anonymous"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Home Page</title>
    <style>
        .form-group {
            position: relative;
            margin-bottom: 1.5rem;
        }

        .form-group textarea {
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
            padding: 1rem;
            outline: none;
            background-color: transparent;
            color: #333;
            transition: border-color 0.5s;
            width: 100%;
            resize: none;
            box-sizing: border-box;
        }

        .form-group textarea:focus {
            border-color: #4facfe;
        }

        .form-group label {
            position: absolute;
            top: 50%;
            left: 1rem;
            transform: translateY(-50%);
            transition: transform 0.5s, color 0.5s;
            pointer-events: none;
            background-color: transparent;
            padding: 0 0.25rem;
            color: #555;
        }

        .form-group textarea:focus ~ label,
        .form-group textarea:not(:placeholder-shown) ~ label {
            transform: translateY(-2.5rem) scale(0.75);
            background-color: #fff;
            padding: 0 0.25rem;
            color: #555;
        }

        .btn-primary {
            display: inline-block;
            background-color: #4facfe;
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .btn-primary:hover {
            background-color: #00c6ff;
            transform: translateY(-2px);
        }

        .btn-primary:active {
            background-color: #009ecf;
            transform: translateY(0);
        }
    </style>
</head>
<body> 
    <h2>Edit Achievements Text</h2>
    <form action="/editAchievementsText" method="post">
        <div class="form-group">
            <textarea name="editTextForAchievements" id="editTextForAchievements" placeholder=" " rows="3" class="form-control"></textarea>
            <label for="editTextForAchievements">Update Achievements Text</label>
        </div>
        <input type="submit" class="btn-primary">
    </form>

    <h2>Edit Meet Our Coach Text</h2>
    <form action="/editMeetOurCoachText" method="post">
        <div class="form-group">
            <textarea name="editTextForMeetOurCoach" id="editTextForMeetOurCoach" placeholder=" " rows="3" class="form-control"></textarea>
            <label for="editTextForMeetOurCoach">Update Meet Our Coach Text</label>
        </div>
        <input type="submit" class="btn-primary">
    </form>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
